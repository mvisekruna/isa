package com.project.isa.repository;

import com.project.isa.model.AdventurePromotionUser;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AdventurePromotionUserRepository extends JpaRepository<AdventurePromotionUser, Long> {
}
