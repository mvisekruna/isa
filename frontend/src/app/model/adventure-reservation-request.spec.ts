import { AdventureReservationRequest } from './adventure-reservation-request';

describe('AdventureReservationRequest', () => {
  it('should create an instance', () => {
    expect(new AdventureReservationRequest()).toBeTruthy();
  });
});
